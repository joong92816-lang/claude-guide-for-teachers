# 이 파일을 복사해 .env.local 을 만드세요:  cp .env.local.example .env.local
# .env.local 은 .gitignore 에 포함되어 있으며 절대 커밋하지 마세요.

# Anthropic API 키 (필수) — https://console.anthropic.com 에서 발급
ANTHROPIC_API_KEY=sk-ant-...

# 선택: 모니터링 파이프라인이 PR 초안을 만들 때 사용 (GitHub Actions에서 자동 주입됨)
# GITHUB_TOKEN=
